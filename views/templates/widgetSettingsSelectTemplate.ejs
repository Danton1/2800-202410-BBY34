<select id="<%= id %>" class="bg-gray-600 text-white border border-gray-500 rounded p-2">
    <% options.forEach(option => { %>
        <option value="<%= option.value %>"><%= option.label %></option>
    <% }); %>
</select>